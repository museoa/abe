#ifndef MONSTER_H 
#define MONSTER_H

#include "Main.h"

typedef struct _monster {
  int image_count;
  Image *images[256];
} Monster;

void initMonsters();
void addMonsterImage(int monster, Image *image);

#endif
